on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test
        uses: ./
        with:
          vpn_profile: ${{ secrets.VPN_PROFILE }}
